# Root environment file — placeholders only.
# Per-app environment variables are documented in:
#   apps/web/.env.example
#   apps/api/.env.example
#   apps/worker/.env.example

# Default local infra connection strings (used by scripts)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/prediction
REDIS_URL=redis://localhost:6379

# Chain to operate on locally: 4663 (mainnet), 46630 (testnet)
CHAIN_ID=46630