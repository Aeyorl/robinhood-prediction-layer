import { PagePlaceholder } from "@/components/page-placeholder";

export default function CommunitiesPage() {
  return (
    <PagePlaceholder
      title="Communities"
      description="Analytics grouped by the original source funding token — volume in USDG, participating wallets, per-market YES/NO split, hit rate, and top predictors."
      milestone="Phase 6 — community layer"
    />
  );
}
