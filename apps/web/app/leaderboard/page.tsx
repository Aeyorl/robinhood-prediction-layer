import { PagePlaceholder } from "@/components/page-placeholder";

export default function LeaderboardPage() {
  return (
    <PagePlaceholder
      title="Leaderboard"
      description="Top predictors by resolved-market performance (hit rate, realized PnL, normalized volume)."
      milestone="Phase 6"
    />
  );
}
