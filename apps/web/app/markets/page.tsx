import { PagePlaceholder } from "@/components/page-placeholder";

export default function MarketsPage() {
  return (
    <PagePlaceholder
      title="Markets"
      description="Browse open, locked, resolved, and cancelled binary markets on objective financial outcomes."
      milestone="Phase 2/3 — directory feeds from the indexer"
    />
  );
}
