import { PagePlaceholder } from "@/components/page-placeholder";

export default function NewMarketPage() {
  return (
    <PagePlaceholder
      title="New market"
      description="Wizard: asset → feed → latest oracle status → comparator → strike → times → fee → exact generated question → exact onchain params → contract transaction preview."
      milestone="Phase 2/5"
    />
  );
}
