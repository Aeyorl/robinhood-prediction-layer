import { PagePlaceholder } from "@/components/page-placeholder";

export default function AdminPage() {
  return (
    <PagePlaceholder
      title="Admin"
      description="Wallet-signed admin session with onchain role checks: create template markets, review oracle health, and audit existing market terms (immutable after creation)."
      milestone="Phase 2/5"
    />
  );
}
