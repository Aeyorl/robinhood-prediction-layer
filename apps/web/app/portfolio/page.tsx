import { PagePlaceholder } from "@/components/page-placeholder";

export default function PortfolioPage() {
  return (
    <PagePlaceholder
      title="Portfolio"
      description="Active, claimable, resolved, and historical positions with side, normalized stake, original funding token, pool share, and claim state."
      milestone="Phase 2/3 — requires indexer projections"
    />
  );
}
