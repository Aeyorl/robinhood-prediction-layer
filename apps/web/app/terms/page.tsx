import { PagePlaceholder } from "@/components/page-placeholder";

export default function TermsPage() {
  return (
    <PagePlaceholder
      title="Terms of use"
      description="Terms acceptance is a compliance hook for production. Policy text is intentionally not invented here — see docs/security.md for the compliance plan."
      milestone="Pre-launch legal review"
    />
  );
}
